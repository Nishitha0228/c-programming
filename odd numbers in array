#include <stdio.h>
void main() {
    int arr[]={1,2,3,4,5,6,7,8,9};
    int l=sizeof(arr)/sizeof(arr[0]);

    for (int i=0;i<l;i++){
        if(arr[i]%2==1){
            printf("Odd Element: %d \n",arr[i]);
        }
    }
    
}
